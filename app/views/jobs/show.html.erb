<html>
<body>
<section class="detail-panel">
  <div class="job-detail">
    <h1> <%= @job.title %> </h1>

  <p><%= simple_format(@job.description) %></p>

  <div class="apply-this-vacancy">
    <%= link_to("Apply Now", new_job_resume_path(@job), :target => "_blank", :class => "btn btn-default") %>
  </div>
  </div>
</section>
</body>
</html>
